spring:
  datasource:
    url: jdbc:mysql://localhost:3306/keepnote?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: sowmya
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: user-auth
server:
  port: 9100
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka